{
  "name": "sync-in-backend",
  "version": "2.0.0",
  "description": "Search backend for Sync In. Proxies the official YouTube Data API v3 — playback happens client-side via YouTube's IFrame Player API, so this server never extracts or re-serves media streams.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.1.0"
  }
}
